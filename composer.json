{
    "name": "packspec/packspec",
    "description": "packspec-php",
    "license": "MIT",
    "require": {
        "php": ">=7.0",
        "symfony/yaml": "^3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^4.8.35"
    },
    "autoload": {
        "psr-4": {
            "packspec\\packspec\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit --debug --bootstrap tests/autoload.php tests/ --coverage-clover coverage-clover.xml"
    }
}
